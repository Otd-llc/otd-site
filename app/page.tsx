// Placeholder server home — replaced by the v4 hub in Phase 1 (Task 1.2).
// The legacy ssr:false client-only home was retired: Next 16 forbids
// `next/dynamic({ ssr: false })` inside a Server Component.
export default function Home() {
  return null
}
